/*
const dispatcher = {
  create: function(evt) {
    this.evt = evt;
    return this;
  },
  dispatch: function(op, callback) {
    this.evt.send(op.type).with(op.payload).to(op.stream).then( (res, err) => callback(res, err) );
  },
};

module.exports = dispatcher;
*/
